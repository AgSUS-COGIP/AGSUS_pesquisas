NEXT_PUBLIC_SITE_URL=http://localhost:3000

# PostgreSQL da aplicação (schema `sigav`).
# Aceita URL PostgreSQL ou JDBC; usuário e senha podem ser separados.
EMPRESA_DATABASE_URL=
USERNAME_DATABASE_URL=
PASSWORD_DATABASE_URL=

# Opcional quando o ambiente separa o dono do schema da credencial de runtime.
# No db_dataware de desenvolvimento, repita usr_sip_app e a mesma senha.
MIGRATION_USERNAME_DATABASE_URL=
MIGRATION_PASSWORD_DATABASE_URL=

# Use disable somente em servidores confirmados sem TLS, como o ambiente de desenvolvimento atual.
PGSSLMODE=disable

# Autenticação Auth.js / Google OAuth.
# Gere AUTH_SECRET com: openssl rand -base64 32
AUTH_SECRET=
AUTH_URL=http://localhost:3000
AUTH_GOOGLE_HD=agenciasus.org.br
AUTH_GOOGLE_CLIENT_ID=
AUTH_GOOGLE_CLIENT_SECRET=

# E-mail e tarefas agendadas.
SMTP_USER=
SMTP_APP_PASSWORD=
CRON_SECRET=

# --- Control plane de manutencao (Vercel Global Config) ---
# No plano Hobby ha um unico store. Production e Preview usam CHAVES SEPARADAS
# dentro dele: maintenance-production e maintenance-preview.
#
# EDGE_CONFIG e a connection string de LEITURA do store compartilhado.
# A ESCRITA vai pela API oficial da Vercel e usa VERCEL_API_TOKEN.
# VERCEL_ENV decide automaticamente qual chave deve ser lida/escrita.
#
# Nenhuma destas pode virar NEXT_PUBLIC_*: o token de escrita no navegador
# permitiria a qualquer visitante parar a plataforma.
EDGE_CONFIG=
EDGE_CONFIG_ID=
VERCEL_TEAM_ID=
VERCEL_API_TOKEN=
