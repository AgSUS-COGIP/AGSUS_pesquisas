import { PlatformSkeleton } from "@/components/platform-skeleton";

export default function GlobalLoading() {
  return <PlatformSkeleton title="Carregando dados, permissões e módulos institucionais" />;
}
